@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="container mt-4">
    <h2>Welcome, Admin!</h2>
    <p>Use the buttons below to manage your system:</p>

    <div class="d-flex gap-3 flex-wrap">
        <a asp-action="ManagePackages" class="btn btn-primary">Manage Tour Packages</a>
        <a asp-action="CustomerDetails" class="btn btn-secondary">View Customers</a>
        @* <a asp-controller="Account" asp-action="Logout" class="btn btn-danger">Logout</a> *@
    </div>
</div>
